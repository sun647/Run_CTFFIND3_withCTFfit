# Run_CTFFIND3_withCTFfit
This repository provides scripts to run CTFFIND3 from the terminal using multiple parallel processes, along with tools to calculate CTF fit resolution from CTFFIND3 output power spectrum and log files.
